/* eslint-disable import/prefer-default-export */

/**
 * Estrutura pages servira para adicionar arquivos de estilos a páginas
 */
import styled from 'styled-components';

export const container = styled.div``;
